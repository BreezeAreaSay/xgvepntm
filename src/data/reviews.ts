/**
 * Обезличенные отзывы гостей.
 *
 * ⚠️ МАССИВ ПУСТ НАМЕРЕННО. Я не пишу отзывы за гостей: выдуманный отзыв,
 * поданный как настоящий, — это обман посетителя, и для заведения это
 * прямой репутационный и юридический риск.
 *
 * Как заполнить правильно:
 *  1. Возьмите реальные отзывы со страницы заведения в Яндекс Картах.
 *  2. Уберите имена и любые опознавательные детали — оставьте только текст.
 *  3. Не редактируйте смысл: сокращать можно, переписывать нельзя.
 *  4. Рядом всегда держите ссылку на первоисточник (она в contacts.ts) —
 *     чтобы гость мог проверить полную картину, включая критику.
 *
 * Пока массив пуст, блок отзывов показывает только ссылку на Яндекс Карты.
 */

export type Review = {
  id: string;
  /** Текст отзыва без имён и личных данных. */
  text: string;
  /** Оценка от 1 до 5, как в источнике. */
  rating: 1 | 2 | 3 | 4 | 5;
  /** Месяц и год: 'август 2025'. Без точной даты — это тоже обезличивание. */
  period: string;
};

export const reviews: Review[] = [
  // {
  //   id: 'r1',
  //   text: 'Текст реального отзыва, из которого убраны имя и личные детали.',
  //   rating: 5,
  //   period: 'август 2025',
  // },
];

/** Средняя оценка по загруженным отзывам. null — считать нечего. */
export function averageRating(items: Review[]): number | null {
  if (items.length === 0) return null;
  const sum = items.reduce((acc, r) => acc + r.rating, 0);
  return Math.round((sum / items.length) * 10) / 10;
}
